# France Relocation Member Portal - Environment Variables
# Copy this file to .env and adjust values as needed

# Development server port (default: 5173)
VITE_PORT=5173

# API base URL - leave empty to use WordPress REST API
# Only needed if running portal standalone for development
# VITE_API_BASE_URL=http://localhost:8080/wp-json/framt/v1

# Enable debug mode (shows additional error details in development)
# VITE_DEBUG=true

# Note: In production, the portal is served by WordPress and
# uses the WordPress REST API via relative URLs. These environment
# variables are only used during development with `npm run dev`.
